<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:stretchColumns="1,2"
    >

    <TableRow>

        <ImageView
            android:id="@+id/nutrition_photo"
            android:layout_width="180dp"
            android:layout_height="101dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/nutrition_name"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/nutritions"
            android:layout_marginBottom="5dp"/>

        <TextView
            android:id="@+id/nutrition_name"
            android:layout_width="23dp"
            android:layout_height="80dp"
            android:textColor="@android:color/white"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/nutrition_cal"
            app:layout_constraintStart_toEndOf="@id/nutrition_photo"
            app:layout_constraintTop_toTopOf="parent"
            android:layout_marginRight="6dp"/>

        <TextView
            android:id="@+id/nutrition_cal"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toEndOf="@id/nutrition_name"
            android:textColor="@android:color/white"
            app:layout_constraintTop_toTopOf="parent" />

    </TableRow>

</TableLayout>